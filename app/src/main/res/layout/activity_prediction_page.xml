<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/bckg_login"
    android:padding="15dp"
    tools:context="com.example.dryhunch.prediction_page">

  <ImageView
      android:id="@+id/mapimage"
      android:layout_width="match_parent"
      android:layout_height="300dp"
      android:layout_marginTop="200dp"
      android:layout_centerHorizontal="true"/>

  <TextView
      android:id="@+id/pred_text"
      android:layout_width="wrap_content"
      android:layout_height="wrap_content"
      android:layout_below="@id/mapimage"
      android:layout_marginTop="30dp"
      android:layout_centerHorizontal="true"
      android:text="Your Prediction Here"
      android:textStyle="bold"
      android:textColor="@color/white"
      android:textSize="30sp"/>

  <Button
      android:layout_width="match_parent"
      android:id="@+id/advice_btn"
      android:layout_below="@id/pred_text"
      android:layout_marginTop="20dp"
      android:text="Get suggestions"
      android:layout_height="60dp"
      android:background="@drawable/button_box_green_outline"/>

  </RelativeLayout>